import React from 'react'

const style = {
    width: '100vw',
    height: '100vh' 
}

const Loading = (props) => {
    return (
        <div style={style}>加载中...</div>
    )
}

export default Loading;